#!/usr/bin/env bash
set -euo pipefail

echo "[pre-commit] make check..."

if ! command -v make >/dev/null 2>&1; then
  echo "[pre-commit] make not found; please install it" >&2
  exit 1
fi

# Reuse project Makefile (fmt/imports/vet with local GOCACHE)
make check

echo "[pre-commit] OK"
